"""Edge-discovery lab — Phase 3 exploratory studies.

Each script is runnable from the repo root and writes a JSON + Markdown
artifact under ``research/edge_discovery/studies/outputs/``. The
artifacts are committed so the study is reproducible and a future
researcher can diff a re-run against the recorded run.

These are illustrative studies on synthetic fixtures plus the
already-committed CAMPAIGN_005 benchmark numbers — they are not
strategy evidence. See ``docs/research/EDGE_DISCOVERY_LAB_001_PLAN.md``.
"""
