# Free / Local Parity Verifier — Sprint-003 Export Status

**Date:** 2026-05-22 · **Branch:** `infra-free-local-parity-verifier-003-with-data`
**Phase:** 2 · `strategy_evidence: false`

Whether the seven H4 CSV exports were produced from a local SQLite
store via `scripts/export_lean_parity_data.py`. **Status: BLOCKED —
no SQLite store exists locally** (see
[`FREE_LOCAL_PARITY_VERIFIER_003_REHYDRATE_STATUS.md`](FREE_LOCAL_PARITY_VERIFIER_003_REHYDRATE_STATUS.md)).

> No strategy approved. CAMPAIGN_002 remains REJECT. Paper / demo /
> live remain blocked. No orders submitted. No OANDA endpoint
> contacted this sprint.

## Export command

The intended command (per the export script's docstring) is:

```bash
for inst in EUR_USD GBP_USD USD_JPY AUD_USD USD_CAD USD_CHF NZD_USD; do
  python scripts/export_lean_parity_data.py \
    --db data/oanda_h4_research.sqlite3 \
    --instrument "$inst" --from 2020-01-01 --to 2026-05-20
done
```

**Not run on this branch** — the prerequisite SQLite store is absent.

## Source SQLite path

`data/oanda_h4_research.sqlite3` — **absent**. The data directory
contains only `data/.gitkeep`.

The script refuses to run without the source database. The docstring
states: "If no real OANDA H4 store is present, this script exports
nothing and exits non-zero. It never fabricates data."

## Output directory

`research/lean_parity/exports/campaign_002_h4/` — present, but
contains only the seven committed `*_H4_lean.provenance.json`
sidecars. **Zero `*_H4_lean.csv` files** were produced this sprint.

## Per-pair status

| instrument | CSV present? | rows | first ts | last ts |
|---|---|---|---|---|
| EUR_USD | **NO** — not exported | — | — | — |
| GBP_USD | **NO** — not exported | — | — | — |
| USD_JPY | **NO** — not exported | — | — | — |
| AUD_USD | **NO** — not exported | — | — | — |
| USD_CAD | **NO** — not exported | — | — | — |
| USD_CHF | **NO** — not exported | — | — | — |
| NZD_USD | **NO** — not exported | — | — | — |

## Per-pair row counts / timestamp ranges

**N/A** — nothing was exported. The seven committed
`*.provenance.json` files document the expected shape each CSV must
have once regenerated (e.g. EUR_USD: 9,931 H4 candles from
`2020-01-01T22:00:00+00:00` to `2026-05-19T21:00:00+00:00`,
SHA-256 `866d75446030655b…`).

## Generated files not committed

**None.** Nothing was generated by this phase.

## Blockers

1. **No `data/oanda_h4_research.sqlite3` source database.** The
   export script requires it; without it, it exits non-zero by
   design.

   **Resolution:** completing
   [`FREE_LOCAL_PARITY_VERIFIER_003_REHYDRATE_STATUS.md`](FREE_LOCAL_PARITY_VERIFIER_003_REHYDRATE_STATUS.md)
   Phase 1 successfully (either by configuring practice credentials
   or by transferring an existing SQLite from another machine)
   unblocks this phase automatically.

## Safety statement

- **No orders submitted.** The export script has no order endpoints.
- **No OANDA endpoint contacted.** The export script is pure local
  SQLite read.
- **No broker credentials printed.** None of the export-related
  commands touch credentials at all.
- **No `.env` staged.** No `.env` exists locally.
- **No SQLite store staged.** None exists.
- **No candle CSV staged.** None exists.
- **No strategy approved.** `configs/approved_strategies.yaml`
  remains `approved: []`.
- **CAMPAIGN_002 remains REJECT.**
- **Paper / demo / live remain blocked.**
